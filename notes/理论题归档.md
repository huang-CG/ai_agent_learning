# 理论题归档（累计）

> 从历史对话 transcript 汇总；便于新窗口复习，不必翻旧聊天。  
> 规则：零碎自测、可不绑当天课；**不做不算当天未完成**。  
> **参考答案格式**：正确答案 + **为什么选这个、为什么不选其他**（逐选项）。  
> 最后同步：2026-07-23（含 D22 结束布置两题；全量补全解析）

---

## 索引

| 归属 | 题量 | 你的作答 | 结果 |
|------|------|----------|------|
| D7 结束 | 2 | （笔记未记选择） | 见答案 |
| D8 | 2 | B / C | ✅ |
| D9 | 2 | 口述正确 | ✅ |
| D10 | 2 | 感悟/自测 | ✅ |
| D11 | 1 | 口述正确 | ✅ |
| D12 | 2 | 可选补出 | 见下（开放题） |
| D13→D14 | Git 口述 + FC/RAG 选择 | C / A | ✅ |
| D15 结束 | 2 | B / A | ✅ |
| D16 结束 | 2 | B / B | ✅ |
| D17 结束 | 2 | B / B | ✅ |
| D18 结束 | 2 | B / B | ✅ |
| D19 结束（D20 开场做） | 2 | C / B | ✅ |
| D20 结束（D21 开场做） | 2 | B / B | ✅ |
| D21 结束（D22 开场做） | 2 | B / B | ✅ |
| D22 结束（D23 开场做） | 2 | 待做 | 见下方 |

---

## D7 · API / 环境（入门）

**题 1**  
`load_dotenv()` 之后，`os.getenv("DEEPSEEK_API_KEY")` 取到的值最可能来自哪里？

- A. `chat.py` 里写死的字符串  
- B. `.env` 文件里的配置  
- C. DeepSeek 服务器返回的 JSON  
- D. `requests.post` 的响应头  

**答案：B**

**为什么选 B、不选其他**  
- **B ✅**：`load_dotenv()` 把 `.env` 键值加载进环境变量，`os.getenv` 再读取。  
- **A ❌**：Key 不应写死在代码里；写死也不需要 `load_dotenv`。  
- **C ❌**：服务器返回的是回答内容，不是你的本地 Key。  
- **D ❌**：响应头不含你本地配置的 API Key。

---

**题 2**  
`response.raise_for_status()` 在什么时候会抛异常？

- A. `response.json()` 解析失败时  
- B. HTTP 状态码不是 2xx（如 401、404）时  
- C. 网络超时时  
- D. API Key 写错时（无论状态码）  

**答案：B**

**为什么选 B、不选其他**  
- **B ✅**：该方法只检查 HTTP 状态码是否为成功（2xx）。  
- **A ❌**：JSON 解析失败是 `.json()` 抛的，与 `raise_for_status` 无关。  
- **C ❌**：超时通常在 `requests.post(..., timeout=)` 阶段抛，到不了或早于状态码检查。  
- **D ❌**：Key 写错常导致 401，本质仍是「非 2xx」；若请求没发出去则不是这个方法专门检测 Key。

---

## D8 · temperature / 幻觉 ✅

**题 1**  
`temperature=0` 时，同一问题多次调用，回答通常会怎样？

- A. 每次差很多  
- B. 几乎一样、很稳定  
- C. 一定更短  
- D. 一定更长  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：温度越低越「贪婪」、越确定，输出更稳定（工程上未必 100% 字节相同）。  
- **A ❌**：那是高温（如 1.2）更发散的表现。  
- **C ❌ / D ❌**：温度管随机性，不直接决定长短。

---

**题 2**  
下面哪项最能降低 LLM「幻觉」风险？

- A. 把 temperature 调到 2  
- B. 让模型更长回答  
- C. 用 RAG / 检索真实资料再回答  
- D. 不用 system prompt  

**你的答案：C** ✅

**为什么选 C、不选其他**  
- **C ✅**：先检索可靠资料再生成，比只调参数更能减少瞎编。  
- **A ❌**：温度调高更发散，幻觉风险通常增加。  
- **B ❌**：更长回答可能占满上下文，不直接降幻觉。  
- **D ❌**：不用 system 往往更失控，不是降幻觉手段。

---

## D9 · system/user · HTTP404 ✅

**题 1**  
`messages` 里 `system` 和 `user` 一般怎么分工？

**你的答：** system 定规则（角色、格式、约束）；user 放内容；也可通过反复强调达成约束 ✅

**要点补强**  
- system = 岗位说明书 / 全局规矩（通常每轮都带）  
- user = 本次任务与待处理文本  
- 约束主要写在 system；user 里反复强调是辅助，不是替代

---

**题 2**  
HTTP 状态码 `404` 表示什么？

**你的答：** 客户端侧错误码；服务器已联通，但请求的资源不存在 ✅

**要点补强**  
- 4xx = 客户端侧问题；5xx = 服务器内部错误  
- 404 = Not Found：连上了服务器，但路径/资源找不到（常见 URL 拼写错误）  
- 对比：401 未授权、403 禁止、500 服务器错误

---

## D10 · CoT ✅

**Q1：** 为什么说 CoT 有时比降低 `temperature` 更有效？

**答：** temperature 管随机性（选词稳不稳），不教模型拆步骤；CoT 显式要求写推理链，中间步骤成为后续 token 的上下文，对多步题更有帮助。实践常 **CoT + 低 temperature**。

---

**Q2：** CoT 和格式约束的相同点、不同点？能否一起用？

**答：**  
- 相同：都是 Prompt 里的约束性指令  
- 不同：CoT 约束**推理过程**；格式约束约束**输出形态**（JSON、固定最后一行等）  
- 能一起用：例如「逐步思考 + 最后一行写：答案：鸡=X，兔=Y」

---

## D11 · 流式 ✅

**Q：** 流式相比非流式，用户体验和技术实现各改什么？

**答：**  
- **体验**：边生成边显示，不用干等整段  
- **技术**：body 加 `"stream": true`；`requests.post(..., stream=True)`；用 `iter_lines` 读 SSE，取 `delta.content`；`data: [DONE]` 结束（不能一次 `response.json()`）

---

## D12 · 多轮（可选开放题）

**Q1：** 为什么说大模型「本身没有记忆」，但多轮聊天又能记得你叫什么？

**答：** 模型两次 API 请求之间不保存状态。「记得」是因为**你的程序**用 `messages` 保存历史，每轮把整段（含过往对话）再发给 API；模型从**本次请求的上下文**里看到以前说过什么。

---

**Q2：** `trim_history` 为什么一般要「永远保留 system，只裁旧的 user/assistant」？

**答：** 多轮会让 token 膨胀（贵、慢、易超窗口）。`system` 是整场角色与规矩，丢掉会行为漂移；裁掉**最旧的对话轮次**即可控长度，同时保留最近上下文。

---

## D13→D14 · Git 口述 + FC / RAG ✅

### Git（D13 开场口述）

- `git add`：工作区 → 暂存区 / 开始追踪  
- `git commit`：暂存区 → **本地**仓库快照  
- `git push`：本地 → 远程（如 GitHub）

### 选择题（D14 结束布置）

**题 1** 关于 Function Calling，下列哪项**正确**？

- A. 模型收到 `tools` 后会在服务器上直接执行你的 Python 函数  
- B. `arguments` 是你写在请求体里的函数说明书  
- C. 模型返回 `tool_calls` 后，应由程序执行函数，再用 `role: "tool"` 把结果放回 messages 再请求一次  
- D. `finish_reason: "stop"` 表示模型正在等待工具结果  

**你的答案：C** ✅

**为什么选 C、不选其他**  
- **C ✅**：模型只「点菜」；程序执行；`role:tool` 回传后再请求，模型才说人话。  
- **A ❌**：模型不会在远端跑你的本地 Python。  
- **B ❌**：说明书是 `tools` / `parameters`；`arguments` 是模型**填好的参数值**。  
- **D ❌**：`stop` = 已说完；等工具时一般是 `finish_reason: tool_calls`。

---

**题 2** 什么是 **RAG**？

- A. 先把文档切块、做成向量检索，再把相关片段塞进 Prompt 让模型回答，减少胡编  
- B. 把 temperature 调到 0，强制模型只输出 JSON  
- C. 多 Agent 互相打电话的框架名  
- D. 仅指 `git pull` 同步远程仓库  

**你的答案：A** ✅

**为什么选 A、不选其他**  
- **A ✅**：RAG = Retrieval-Augmented Generation（检索增强生成）。  
- **B ❌**：那是低温 / JSON 风格约束，不是 RAG。  
- **C ❌**：那是多 Agent，不是 RAG 定义。  
- **D ❌**：玩梗；与检索增强无关。

---

## D15 结束 · 二次请求 / Embedding ✅

**题 1**  
Function Calling 里，为什么执行完工具后**还要再请求一次** API？

- A. 为了省 token  
- B. 因为第一次只拿到「要调什么函数+参数」，得把工具结果发回去，模型才能生成给用户的自然语言回答  
- C. 因为 API 规定必须请求两次  
- D. 为了让 temperature 生效  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：第一轮是点菜（tool_calls）；回传 Observation/`role:tool` 后，第二轮才说人话。  
- **A ❌**：再请求通常多花 token，不是为了省。  
- **C ❌**：不是「必须两次」的死规定；不调工具时一轮就能 stop。  
- **D ❌**：temperature 每轮请求都可带，与是否二次请求无必然关系。

---

**题 2**  
下面哪个是 **Embedding（向量）** 的作用？

- A. 把文字转成一串数字向量，让「语义相近」的内容在向量空间里也相近，便于检索  
- B. 把模型温度调低  
- C. 压缩图片体积  
- D. 加密 API Key  

**你的答案：A** ✅

**为什么选 A、不选其他**  
- **A ✅**：Embedding 是 RAG 检索的基础（语义相似度）。  
- **B ❌**：那是 temperature。  
- **C ❌**：图片压缩无关。  
- **D ❌**：Key 安全靠 `.env` / 权限，不是 Embedding。

---

## D16 结束 · top-k / Embedding 与 DeepSeek ✅

**题 1**  
`top-k`（RAG）和 `top_p`（API）的区别？

- A. 一样，都是取前几个候选  
- B. top-k 管检索取几块文档；top_p 管生成时怎么抽样 token  
- C. top-k 只用于 temperature；top_p 只用于 Embedding  
- D. 都是向量库名字  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：同名不同物——检索 top-k vs 生成采样 top_p。  
- **A ❌**：场景不同，不能当成同一个参数。  
- **C ❌**：top-k 不是 temperature 专用；top_p 也不是 Embedding 参数。  
- **D ❌**：都不是向量库产品名。

---

**题 2**  
为什么「高质量」知识库不能只配 DeepSeek Chat？

- A. DeepSeek 太贵  
- B. Chat 模型负责生成回答，Embedding 负责把文字变成向量；DeepSeek API 主要提供 LLM，还需单独配 Embedding  
- C. Dify 强制要求 OpenAI  
- D. 向量库只能用 FAISS  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：LLM ≠ Embedding；高质量向量检索要单独配嵌入模型（如硅基流动 bge-m3）。  
- **A ❌**：贵不贵不是「不能只配 Chat」的本质原因。  
- **C ❌**：Dify 可换模型，不强制 OpenAI。  
- **D ❌**：向量库有多种选择，不是只能 FAISS。

---

## D17 结束 · 拼 Prompt / Agent 特点 ✅

**题 1**  
关于「拼 Prompt」知识库，下列哪项**正确**？

- A. 文档越大越好，全部塞进 Prompt 永远没问题  
- B. 适合小文档；文档过长易占满上下文、浪费 token  
- C. 必须先 Embedding 再问答  
- D. 和 RAG 完全一样，只是名字不同  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：拼 Prompt 简单，但大文档易爆上下文窗口。  
- **A ❌**：越大越容易超窗口、贵且慢。  
- **C ❌**：拼 Prompt 恰恰可以不 Embedding；那是 RAG 路径。  
- **D ❌**：RAG 有检索选片段；拼 Prompt 是整份硬塞，不一样。

---

**题 2（预习 Phase 2）**  
下面哪项最接近 **AI Agent** 的特点？

- A. 只能一次性回答一个固定问题，不能用工具  
- B. 能根据目标多步推理，必要时调用工具/环境，再根据结果继续行动  
- C. 专指向量数据库软件  
- D. 等于把 temperature 调到 0  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：感知-推理-决策-执行，可循环至目标完成。  
- **A ❌**：那更像单轮 Chatbot。  
- **C ❌**：向量库是组件，不是 Agent 定义。  
- **D ❌**：temperature 只是生成参数。

---

## D18 结束 · FC 在 Agent 中的位置 / Observation ✅

**题 1**  
Agent 四步循环里，Function Calling 主要落在哪一环？

- A. 只在「感知」  
- B. 「决策」选工具 + 「执行」由程序真正调用  
- C. 只在「推理」，且模型会自己执行 Python  
- D. 与 Agent 无关  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：模型决策点哪个工具；程序执行；结果再感知。  
- **A ❌**：感知更接近拿到工具结果之后。  
- **C ❌**：模型不执行你的 Python。  
- **D ❌**：FC 是 Agent 用工具的核心机制之一。

---

**题 2（预习 ReAct）**  
ReAct 里的 **Observation** 最接近什么？

- A. 用户的第一句提问  
- B. 模型调用工具后返回的结果（供下一轮思考）  
- C. 把 temperature 调高  
- D. git commit 的说明文字  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：Observation ≈ 工具返回，供下一轮 Thought。  
- **A ❌**：那是 Question / user。  
- **C ❌**：与 Observation 无关。  
- **D ❌**：无关。  
- **补充**：真 Agent 里 Observation 由**程序执行工具**产生（见 D19 题），不是 LLM 编的。

---

## D19 结束（D20 开场已做）· Observation 谁产生 / description ✅

**题 1**  
ReAct 循环里，**Observation** 的内容通常由谁产生？

- A. 用户手动输入  
- B. LLM 自己想象  
- C. 程序执行工具（API/函数）后返回的真实结果  
- D. git 自动生成  

**你的答案：C** ✅

**为什么选 C、不选其他**  
- **C ✅**：LLM 写 Thought/Action；程序跑工具后得到 Observation。  
- **A ❌**：用户只提问题，不负责填 Observation（纸笔练习可假装，真 Agent 不是）。  
- **B ❌**：让模型编 Observation = 失去「行动」意义。  
- **D ❌**：与 git 无关。

---

**题 2（预习 D20）**  
工具定义里 **description** 写得好坏，主要影响什么？

- A. 工具执行速度  
- B. 模型能否在合适时机选对工具、填对参数  
- C. API Key 的安全性  
- D. Python 版本兼容性  

**你的答案：B** ✅

**为什么选 B、不选其他**  
- **B ✅**：description 是给模型的菜单说明（何时用/何时不用）；参数 description 影响怎么传。  
- **A ❌**：执行速度取决于函数/网络，不取决于文案。  
- **C ❌**：Key 安全靠环境变量与权限。  
- **D ❌**：与 Python 版本无关。

---

## D20 结束（D21 开场做 · 预习手写 ReAct 循环）

**题 1**  
D21 要先实现「不含真实工具」的 ReAct 主循环，这时 **Observation** 一般怎么处理？

- A. 完全不要 Observation，只写 Thought  
- B. 程序先用**模拟/假数据**充当 Observation（像 D19 纸笔练习）  
- C. 让 LLM 自己编造 Observation 并当作真实工具结果  
- D. 必须等用户每次手动输入 Observation  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：D21 先练循环 + 解析；用 `simulate_tool` 假数据即可；D22 再接真 API。  
- **A ❌**：没有 Observation，ReAct「行动→感知」环不完整，也练不到回传。  
- **C ❌**：让模型编结果 = 自说自话，学不到工具回传。  
- **D ❌**：不必每次人工输入；程序模拟即可。

**你的答案：B** ✅

---

**题 2（预习 D21）**  
手写 ReAct Agent 为什么要加 **`max_steps`（最大步数）**？

- A. 只是为了省 API 费用  
- B. 防止模型一直循环 Thought/Action/Observation、迟迟不输出 Final Answer，造成死循环  
- C. 限制用户一次只能问一句话  
- D. Python 语法强制要求  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：和 D15 `max_tool_rounds` 同类——给循环设上限。  
- **A ❌**：顺带可能少花请求，但主要目的是防死循环，不是「只为省钱」。  
- **C ❌**：限制的是 Agent 内部步数，不是用户聊天句数。  
- **D ❌**：语法不强制；是工程上的保险丝。

**你的答案：B** ✅

---

## D21 结束（D22 开场做 · 复习 + 预习）

**题 1（复习 D21）**  
手写 ReAct 里，**scratchpad** 的主要作用是？

- A. 存放 API Key  
- B. 累加每轮 Thought / Action / Observation，下一轮再喂给模型当上下文  
- C. 替换 system prompt  
- D. 自动把 max_steps 改成无限  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：scratchpad 把每轮 T/A/O 拼成字符串，下一轮塞进 user，模型才能看见做过什么。  
- **A ❌**：API Key 在 `.env` / 环境变量里。  
- **C ❌**：system 是岗位说明书，一般固定；scratchpad 是过程记录。  
- **D ❌**：max_steps 是循环上限，不会被 scratchpad 改成无限。

**你的答案：B** ✅

---

**题 2（随机八股 / 预习 D22）**  
D22 把 `simulate_tool` 换成真实天气 API 后，下列哪项正确？

- A. Observation 仍应由 LLM 自己编造，不必调 HTTP  
- B. Observation 应来自程序执行 `requests`（或同类）后的真实返回  
- C. 有了真 API 就不再需要 `max_steps`  
- D. Action 改为由用户手动输入工具名  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：模型只输出 Action；程序调 wttr.in / 计算器后，把真实结果写成 Observation。  
- **A ❌**：LLM 编 Observation = 胡编天气，失去「行动」。  
- **C ❌**：真 API 仍可能反复失败或一直 Action；`max_steps` 仍要保留。  
- **D ❌**：Action 仍由模型决定；用户只提问。

**你的答案：B** ✅

---

## D22 结束（D23 开场做 · 复习八股 + 随机）

**题 1（复习 · HTTP）**  
调天气 API（wttr.in）和调 DeepSeek Chat，HTTP 方法上通常分别是？

- A. 都是 POST  
- B. 天气查询常用 GET；Chat Completions 常用 POST  
- C. 都是 GET  
- D. 天气用 PUT；Chat 用 DELETE  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：D4 天气是 `requests.get` + URL 参数；D7 Chat 是 `requests.post` + JSON body（含 messages）。  
- **A ❌**：查天气一般不必 POST 整个 body。  
- **C ❌**：发 messages/tools 的对话接口几乎都是 POST。  
- **D ❌**：与本课用法无关。

**你的答案：B** ✅

---

**题 2（随机八股 · 数据结构）**  
大量做「这个元素见过没有」的判断时，为什么常把 list 改成 set？

- A. set 一定比 list 更省内存  
- B. set 的 `in` 平均接近 O(1)，list 的 `in` 是 O(n)，数据量大时 list 易超时  
- C. set 能自动排序  
- D. set 允许重复元素，查找更准  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：D11 力扣 217、D19 349 都踩过 list `in` 太慢的坑。  
- **A ❌**：set 往往更费内存，换的是速度不是省内存。  
- **C ❌**：set 无序（不保证排序）。  
- **D ❌**：set **去重**、不允许重复；说反了。

**你的答案：B** ✅

---

## D23 结束（零碎自测 · 随机八股）

> 出题规则：随机、不必绑当天课；学过/未学均可。本批按规则重出（替换原先绑 D23/D24 的两题）。

**题 1（复习 · Prompt）**  
想让模型输出更稳、更可复现时，通常应怎样调 `temperature`？

- A. 尽量调高（接近 1 或更高）  
- B. 尽量调低（接近 0）  
- C. temperature 只影响网速，与输出无关  
- D. 必须设为负数才稳定  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：temperature 低 → 更偏向高概率词，输出更稳（D8 实验过）。  
- **A ❌**：调高更随机、更发散，不适合要稳定格式/事实时。  
- **C ❌**：与网速无关，直接影响采样随机性。  
- **D ❌**：一般取值 ≥ 0，没有「必须负数」。

**你的答案：B** ✅

---

**题 2（随机八股 · 数据结构）**  
Python 里下列哪句关于 `dict` 的说法正确？

- A. `dict` 的键可以是 list  
- B. `dict` 用键查找平均接近 O(1)，适合「按名字/字符计数」这类题  
- C. `dict` 和 `list` 一样，只能按下标 `0,1,2…` 访问  
- D. `dict` 不允许修改已有键的值  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：哈希表按键定位；387/383/242 计数都靠这个。  
- **A ❌**：键须可哈希；`list` 不行，常用 `str`/`int`/`tuple`。  
- **C ❌**：`dict` 按键访问，不是按下标顺序（虽 3.7+ 保插入序）。  
- **D ❌**：可以改：`d[k] = new_value`。

**你的答案：A** ❌（正确答案 B）

---

## D24 结束（零碎自测 · 随机八股）

> 出题前已对照协作约定：随机、不必绑当天课；学过/未学均可。

**题 1（复习 · API）**  
流式输出（SSE）里，客户端一般根据什么判断「这一轮内容发完了」？

- A. 收到 HTTP 状态码 404  
- B. 收到特殊标记（如 data 里的 `[DONE]`）或流结束  
- C. temperature 自动变成 0  
- D. 必须收到 Final Answer: 这行字  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：D11 流式：逐条读 `data:`，常见以 `[DONE]` 或连接结束表示完。  
- **A ❌**：404 是错误，不是正常结束信号。  
- **C ❌**：temperature 与流是否结束无关。  
- **D ❌**：那是 ReAct 文本格式，不是 SSE 协议约定。

**你的答案：B** ✅

---

**题 2（随机八股 · Python）**  
关于 `list` 与 `tuple`，哪句正确？

- A. `tuple` 创建后可以随意 `append` 元素  
- B. `tuple` 不可变，常用作 `dict` 的键；`list` 可变，一般不能当 `dict` 键  
- C. `list` 和 `tuple` 都不能用下标访问  
- D. `tuple` 只能装数字，不能装字符串  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：不可变 → 可哈希 → 可作键；`list` 可变不可作键（与 D24 理论题 dict 键呼应）。  
- **A ❌**：`tuple` 无 `append`；要改得先转 `list`。  
- **C ❌**：两者都可用下标 `t[0]`。  
- **D ❌**：可装任意对象（含 str）。

**你的答案：B** ✅

---

## D25 结束（零碎自测 · 随机八股）

> 出题前已对照协作约定：随机、不必绑当天课。

**题 1（复习 · Git）**  
工作区改完文件后，正确的提交顺序通常是？

- A. `git push` → `git commit` → `git add`  
- B. `git add` → `git commit` →（需要时再）`git push`  
- C. 只需要 `git push`，会自动 commit  
- D. `git commit` 可以不经 `add` 就包含所有新文件（含未跟踪）  

**参考答案：B**

**为什么选 B、不选其他**  
- **B ✅**：先暂存（add）再提交（commit）；push 是发到远程，可另做。  
- **A ❌**：顺序反了。  
- **C ❌**：push 不会自动生成 commit。  
- **D ❌**：未跟踪文件一般要先 `add`；`commit -a` 也不包含 untracked。

---

**题 2（随机八股 · 复杂度）**  
在长度为 n 的 `list` 上，反复执行 `x in nums`（共约 n 次），最坏时间复杂度更接近？

- A. O(1)  
- B. O(n)  
- C. O(n²)  
- D. O(log n)  

**参考答案：C**

**为什么选 C、不选其他**  
- **C ✅**：单次 `in` 最坏 O(n)，做 n 次 → O(n²)（268 用 list 查缺失的坑）。  
- **A/D ❌**：不是哈希/折半。  
- **B ❌**：那是「只查一次」或改用 set 后整体量级。

---

## 协作约定（防再丢题）

1. **出题前先读本节约定 + CONTEXT「每日刷题·理论题」规则**（随机、不必绑当天课、学过/未学均可）；读完再出题。  
2. 每天出理论题后，**同步追加本文件**（题干 + 选项 + 参考答案）。  
3. **参考答案必须含「为什么选这个、为什么不选其他」**（逐选项简述），不能只写字母。  
4. 学员只报「选 C」时：Agent **先查本文件 / transcript**，**禁止自拟另一套题当面批改**。  
5. 新窗口复习：优先 `@notes/理论题归档.md`，不必整本 `@学习笔记.md`。  
6. 出题规则：随机、不必绑当天课；学过/未学均可；不做不算当天未完成。  
7. 对话里出题须贴**完整题干 + A/B/C/D 选项**，禁止只甩题干摘要。
