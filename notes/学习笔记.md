# AI Agent 工程师 · 学习笔记

> 你的个人笔记，每日学习后更新  
> 开始日期：**2026-07-01** | 当前：**Day 9 ✅ → Day 10 待开始**

---

## 学习进度

| Day | 日期 | 时长 | 主题 | 完成 |
|-----|------|------|------|------|
| 1 | 2026-07-01 | 3h | 启动日 | ✅ |
| 2 | 2026-07-02 | 3h | Python 语法（一） | ✅ |
| 3 | 2026-07-03 | 3h | Python 语法（二） | ✅ |
| 4 | 2026-07-04 | 3h | Python 进阶 | ✅ |
| 5 | 2026-07-05 | 4h | Git 与项目结构 | ✅ |
| 6 | 2026-07-06 | 3h | 面向对象 · OOP 通讯录 | ✅ |
| 7 | 2026-07-07 | 2h | 首次 LLM API 调用 | ✅ |
| 8 | 2026-07-08 | 3h | LLM 基础概念 | ✅ |
| 9 | 2026-07-09 | 2h | Prompt 基础 | ✅ |
| 10 | — | — | 思维链 CoT | ⬜ |

---

## Day 9 · Prompt 基础（2026-07-09）✅

**时长**：2h | **自评**：4/5 | **Agent 评**：4/5

**Agent 评语**：D9 核心验收通过；`system`/`user` 理解正确，5 种 Prompt 全跑通，能写出「格式约束 vs temperature」的个人理解。2h 版略过编程导航通读与 Few-shot（延至 D10）。力扣两数之和经提示掌握 `range()`、应用 `i != j` 修正示例 3 `[3,3]`；暴力版应可 AC，dict 一遍解法待巩固。自评 4 合理，Agent 评 **4**。

### 今日进度
- [x] 理解 `system` / `user` 分工
- [x] 跑通 5 种 Prompt：翻译 / 摘要 / 分类 / 提取 / 生成
- [x] 对比实验：分类去掉「不要解释」仍只输出一个词
- [x] 笔记：5 个模板 + 个人理解（见下）
- [x] 力扣：1. 两数之和（暴力版，`range` + `i!=j`）
- [ ] Few-shot 系统练习（延至 D10）
- [ ] `git commit` + `git push`（day09 + 笔记 + D8 遗留）

### 关键结论
1. **`system`**：定规矩——角色、输出格式、约束；整段对话生效
2. **`user`**：这一次的具体任务 + 待处理内容
3. **好 Prompt 四要素**：角色 → 任务 → 约束 → 输入
4. **格式类任务**（分类/提取/JSON）应用 `temperature=0.3` 等偏低值，但**不能替代** Prompt 约束
5. **`temperature` vs `system`**：前者管「随机性」，后者管「Instructions」；两件事，要配合用

### 5 个 Prompt 模板

**1. 翻译**
```
system: 你是专业翻译。只输出英文译文，不要解释。
user:   把下面中文翻译成英文：{文本}
```

**2. 摘要**
```
system: 你是摘要助手。用不超过 50 字概括要点。
user:   请摘要：\n{文章}
```

**3. 分类**
```
system: 你是文本分类器。只输出一个标签：正面、负面或中性。不要解释。
user:   {待分类句子}
```

**4. 提取**
```
system: 你是信息提取助手。提取姓名、公司、职位。只输出 JSON：{"name":"","company":"","title":""}
user:   {文本}
```

**5. 生成**
```
system: 你是 Python 助教。生成简短示例代码，并加一行注释说明。
user:   {需求描述}
```

### 实验观察
- 5 种 Prompt 均输出符合约束；分类最稳（只出一个词）；提取为标准 JSON
- 去掉分类 Prompt 里「不要解释」后，本次仍只输出「中性」——说明简单任务 + 低 temperature 也可能听话，但**显式约束仍是保险**

### 我自己的理解（D9）

1. **想让模型 / Agent 在某一类任务上表现稳定时**，应在 Prompt 的 `system` 里写清角色、输出格式和约束；`user` 放本次要处理的具体内容。不只 Agent 才这样，所有 Chat Completions 都适用。

2. **有明确格式要求时，必须写进 `system`（或 `user` 里反复强调）**，不能只指望调低 `temperature` 让模型「自觉听话」。  
   - `temperature` 管的是采样随机性（选词多随机），**不管**「必须 JSON」「只输出一个词」这类格式  
   - 即使 `temperature=0`，复杂任务仍可能啰嗦、格式跑偏  
   - 正确做法：**Prompt 约束格式 + 必要时低 temperature**，两者配合，不能互相替代

### 代码/实验
- 说明：`exercises/day09/README.md`
- 代码：`exercises/day09/prompt_lab.py`
- 运行：`python exercises/day09/prompt_lab.py`

### 力扣 · 两数之和
- 暴力：`for i in range(len(nums))` + `for j in range(i+1, len(nums))`
- 坑：`nums[i] != nums[j]` 会错杀 `[3,3] target=6`；应比下标 `i != j`
- 语法：`!` 不是 Python 取反；`for i in len(nums)` 错，要用 `range()`
- 进阶：dict 一遍 `{值: 下标}`，查 `target - n`（待练）

### 2h 压缩说明
- 保留：5 种 Prompt 实战 + system/user + 笔记模板
- 延后：编程导航通读、Few-shot → D10

### 明日预习
- D10：Chain-of-Thought（CoT）· 普通 Prompt vs 逐步推理对比

---

## Day 8 · LLM 基础概念（2026-07-08）✅

**时长**：3h | **自评**：4/5 | **Agent 评**：4/5

**Agent 评语**：Phase 1 首日验收通过；能用自己的话解释 LLM/Token/上下文窗口/幻觉，并完成 `temperature` 对比实验（0 / 0.7 / 1.2）。`try/except` 在 REPL 自测通过（ValueError、ZeroDivisionError）；力扣 Fizz Buzz 一次写对、`elif` 判断顺序正确。理论题规则调整为可不绑当天课；D8 理论题留零碎时间。自评 4 合理，Agent 评 **4**。

### 今日进度
- [x] LLM 四概念：LLM、Token、上下文窗口、幻觉（口述验收）
- [x] `temperature` 实验：`chat.py` 增加参数 + `test_temperature()`
- [x] 语法补漏：`try/except`（`ValueError` / `ZeroDivisionError`）
- [x] 力扣：412 Fizz Buzz ✅
- [x] 刷题规则：理论题可不绑当天课，可混未学八股
- [x] 理论题 2 道（D8，见下方详解）
- [ ] `git commit` + `git push`（D8 chat.py + 笔记 + CONTEXT）

### 关键结论
1. **LLM**：根据上文预测下一个 Token；不是查库，是生成式模型
2. **Token**：模型处理文本的最小单位；影响计费、长度、速度（生成更长更久）
3. **上下文窗口**：模型一次能看的最大 Token 数；超出会丢前文
4. **幻觉**：模型生成看似合理但不一定为真的内容；信息够时也可能编
5. **temperature**：越低越稳定确定，越高越发散有创意，幻觉风险也更高

### temperature 实验观察（「用三句话介绍广州」）

| temperature | 观察 |
|-------------|------|
| 0 | 最简练稳定：南国之都、食在广州、地标列举 |
| 0.7 | 信息更多：省会、穗/花城、海上丝绸之路等 |
| 1.2 | 表述更丰富：历史底蕴、现代活力、粤剧粤语等 |

### 代码/实验
- 文件：`exercises/day07/chat.py`（D8 在 D7 基础上加 `temperature`）
- 运行实验：临时改入口为 `test_temperature()`
- 廖雪峰错误处理：https://liaoxuefeng.com/books/python/error-debug-test/error/index.html

### 遇到的问题
- 旧链接 `/books/python/error/` 404 → 新路径 `error-debug-test/error/`

### 力扣 · Fizz Buzz
- 要点：先判断 `%3 and %5`，再 `%3`，再 `%5`，否则 `str(i)`
- 下一题预告：[1. 两数之和](https://leetcode.cn/problems/two-sum/)

### 理论题（D8，已完成）

1. **`temperature=0` 多次调用** → **B** 几乎一样、很稳定  
   - 原因：接近贪婪解码，倾向选概率最高的 Token  
   - 工程上未必 100% 相同（硬件、实现等因素）

2. **降低幻觉** → **C** RAG / 检索真实资料再回答  
   - A 调高 temperature 会增加发散和幻觉风险  
   - B 更长回答可能占满上下文，并不直接降幻觉  
   - D 系统工程（system prompt、工具、RAG 等）整体有助于降幻觉，但「不用 system prompt」本身不是办法

### 明日预习
- D9：Prompt 基础（翻译/摘要/分类/提取/生成）
- 验收：5 个 Prompt 模板存入笔记

---

## Day 7 · 首次 LLM API 调用（2026-07-07）✅

**时长**：2h | **自评**：4/5 | **Agent 评**：4/5

**Agent 评语**：D7 核心验收通过；独立完成 `ask_ai()` + `main()` 循环问答，`quit` 退出与异常处理到位。过程中全角括号、`completins` 拼写、`uesr` 角色名等小问题经提示修正；课后主动追问 `Accept` 头、`os.getenv`、`raise_for_status`、模型 ID 与文档差异等，理解到位。2h 完成 Phase 0 收官任务，自评 4 合理，Agent 评 **4**。

### 今日进度
- [x] 实现 `ask_ai()`：`POST` DeepSeek Chat Completions
- [x] 实现 `main()`：循环问答 + 空问题跳过 + `quit`/`exit` 退出
- [x] `try/except requests.RequestException` 网络错误处理
- [x] 验收：问了 list/tuple、AI Agent，正常退出
- [x] 代码注释：旧模型名 `deepseek-chat` 与 V4 新名对应关系
- [x] `git commit` + `git push`：`7937a2a Day07：DeepSeek chat CLI, notes and Phase 0 complete`

### 关键结论
1. **D4 GET → D7 POST**：调 LLM 用 `requests.post`，要带 `Authorization` 和 JSON body（`model` + `messages`）
2. **回答路径**：`response.json()["choices"][0]["message"]["content"]`
3. **Key 管理**：写在 `.env`，`load_dotenv()` + `os.getenv()` 读取，不写死在代码里
4. **`raise_for_status()`**：`requests` 返回的 `Response` 对象方法；HTTP 非 2xx 抛异常，配合 `except` 友好报错
5. **模型 ID**：`deepseek-chat` 是旧版兼容名，官方主推 `deepseek-v4-flash` / `deepseek-v4-pro`；D7 用旧名可跑通

### D4 天气 API → D7 DeepSeek API

| D4 | D7 |
|----|-----|
| `requests.get(url)` | `requests.post(url, json=body)` |
| 无鉴权 | `Authorization: Bearer <KEY>` |
| 参数在 URL | 参数在 JSON body |
| `r.json()` 取天气字段 | `r.json()["choices"][0]["message"]["content"]` |
| `input` 城市名 | `input` 用户问题 |

### 数据流（一次问答）

```
input 问题 → ask_ai 组 url/headers/body → POST DeepSeek
→ raise_for_status 检查状态码 → .json() 解析
→ 取 choices[0].message.content → print 打印 → 回到 while 循环
```

### 代码/实验
- 文件：`exercises/day07/chat.py`
- 说明：`exercises/day07/README.md`
- 运行：`python exercises/day07/chat.py`
- 环境：`python scripts/check_env.py`

### 遇到的问题
- 全角括号 `）` → `SyntaxError` → 改成半角 `)`
- URL 拼写 `completins` → 404 → 改为 `completions`
- `role: "uesr"` → 改为 `"user"`
- 文档里找不到 `deepseek-chat` → 官方已主推 V4 模型名，旧名在兼容说明里

### Phase 0 复盘（D1–D7）
7 天从零搭好 Python 环境：计算器（D2）、函数版通讯录（D3）、天气 API（D4）、Git 协作（D5）、OOP 通讯录（D6），今天第一次用 Python 调通 DeepSeek 大模型。最难的是语法细节（缩进、拼写、全角符号）和 D6 的 dict/Contact 转换；HTTP 请求套路 D4 和 D7 一脉相承。D8 起进入 Phase 1，学 Token、temperature 和 Prompt。

### 明日预习
- D8：LLM 基础概念（Token、上下文窗口、幻觉）
- 实验：同一问题不同 `temperature` 对比
- 验收：用自己的话解释 Token 和幻觉

---

## Day 6 · 面向对象 · OOP 通讯录（2026-07-06）✅

**时长**：3h | **自评**：4/5 | **Agent 评**：4/5

**Agent 评语**：OOP 通讯录验收通过并完成 commit/push；能把 D3 函数版迁移为 `Contact` + `AddressBook`，最终实现 dict ↔ Contact 转换、`contact.show()`、JSON 持久化。过程中 `find` 拼写、`list_all` 逻辑、Contact 与 dict 混用等问题经提示修正；对 `with open(..., "w")` 的文件写入含义主动追问，理解到位。自评 4 合理，Agent 评 **4**。

### 今日进度
- [x] 创建 `exercises/day06/README.md` 与 `address_book_oop.py`
- [x] 完成 OOP 通讯录：`Contact` + `AddressBook`
- [x] 增 / 删 / 查 / 显示全部功能自测通过
- [x] JSON 持久化通过：退出再运行数据仍在
- [x] 加分：内存使用 `Contact` 对象，读写 JSON 时做 dict ↔ Contact 转换
- [x] `git commit` + `git push`：`4ec6bf1 Day06：add OOP address book with Contact objects`
- [x] D7 预习：DeepSeek Chat Completions 请求结构

### 关键结论 · OOP
1. **类（Class）**：把数据和操作绑在一起；D6 中 `Contact` 管单个联系人，`AddressBook` 管联系人列表和文件
2. **`__init__`**：创建对象时自动调用；`self.name` / `self.phone` 是当前对象自己的属性
3. **`self.contacts`**：联系人列表放在 `AddressBook` 对象内部，不再像 D3 一样到处传 `contacts`
4. **JSON 不能直接保存自定义对象**：文件里保存 dict；程序运行时使用 `Contact` 对象
5. **转换链路**：`load` 时 dict → `Contact`；`save` 时 `Contact` → dict

### D3 函数版 → D6 OOP 版

| D3 | D6 |
|----|----|
| `load_contacts()` | `AddressBook.load()` |
| `save_contacts(contacts)` | `AddressBook.save()` |
| `add_contact(contacts)` | `AddressBook.add()` |
| `delete_contact(contacts)` | `AddressBook.delete()` |
| `find_contact(contacts)` | `AddressBook.find()` |
| `list_contacts(contacts)` | `AddressBook.list_all()` |
| `{"name": ..., "phone": ...}` | `Contact(name, phone)` |

### 代码/实验
- 文件：`exercises/day06/address_book_oop.py`
- 说明：`exercises/day06/README.md`
- 数据：`exercises/day06/contacts.json`
- 运行：`python exercises/day06/address_book_oop.py`
- 验收：添加联系人 → 显示/查找/删除 → 退出再运行数据仍在

### 遇到的问题
- 编辑器中写完但未保存 → 磁盘仍是 `pass`，运行读不到新代码
- `find` 中 `phonr` 拼写错误 → 改为 `phone`
- `list_all` 误写成按姓名查找 → 改为直接遍历全部联系人
- 优化时混用 `Contact` 对象和 dict → 改为内存统一用 `Contact`，JSON 读写时转换
- 对 `with open(self.data_file, "w", encoding="utf-8") as f:` 不理解 → 明确其含义是打开文件写入，支持中文，结束自动关闭

### D7 预习 · DeepSeek API

1. **Key 从哪来**：key不能写死在代码里，会有泄露风险，所以应该写在`.env`里，通过os.getenv方法从`.env`里获取
2. **请求发去哪**：`POST {DEEPSEEK_BASE_URL}/chat/completions`（默认 `https://api.deepseek.com`）
3. **回答从哪取**：响应 JSON 里 `choices[0].message.content`

### Chat Completions 请求骨架

- **Headers**：`Authorization: Bearer <API_KEY>` · `Content-Type: application/json` · `Accept: application/json`
- **Body 核心字段**：
  - `model`：如 `deepseek-chat`
  - `messages`：对话列表，例如 `[{"role": "user", "content": "你好"}]`
- **和 D4 天气 API 的相同点**：都是 HTTP POST + JSON；D4 用 `requests.get`，D7 用 `requests.post`

用户输入 → Python 组 messages → POST DeepSeek → 解析 JSON → 打印 content

### 明日预习
- D7：命令行单轮 AI 问答 → `exercises/day07/`
- 先跑 `python scripts/check_env.py` 确认 DeepSeek Key 可用

---

## Day 5 · Git 与项目结构（2026-07-05）✅

**时长**：4h | **自评**：5/5 | **Agent 评**：4/5

**Agent 评语**：D5 验收全过（远程 6 commits、README、笔记）；push/README/笔记 commit 亲手完成；Git 四命令与 OOP 预习、D3 结构口述、D3→D6 映射理解到位。D2–D4 本地 commit 曾由 Agent 代敲；`git long`/`pull` 误敲经纠正；牛客未做。自评 5 略乐观，综合 **4**。

### 今日进度
- [x] 理论：编程导航 Git 路线 · 「猴子都能懂的 Git 入门」（记录变更 → 同步存储库）
- [x] 本地 commit D2–D4（共 4 次 commit，含 D1）
- [x] 创建 GitHub 仓库并 push：[huang-CG/ai_agent_learning](https://github.com/huang-CG/ai_agent_learning)
- [x] 亲手敲：`git log` / `git remote add` / `git push` / `git pull`（误敲 pull 无影响）
- [x] 补全 `README.md` 并 commit + push
- [x] 预习 D6：廖雪峰 OOP 前 3 节 + 终端试 class
- [x] 块 3：回看 `address_book.py` 口述结构
- [x] 块 4：D3 函数版 → D6 类版映射（见下）
- [ ] 牛客语法题 3 道（未做，留零碎时间）

### 关键结论 · Git
1. **`git add`**：暂存修改（含新文件 Untracked → Staged）
2. **`git commit`**：打本地存档，附 `-m "说明"`
3. **`git push`**：本地 → GitHub（上传）
4. **`git pull`**：GitHub → 本地（下载 + 合并）；已是最新则 `Already up to date.`
5. **文件状态循环**：Untracked → add → Staged → commit → Committed → edit → Modified → add → …
6. **三区**：工作区（改文件）· 暂存区（add 后）· 本地仓库（commit 后）；push 同步到远程

### 关键结论 · OOP 预习（D6）
1. **OOP**：以对象为基本单元；**类（Class）= 自定义对象类型**，类是模板，实例是具体对象
2. **`self`**：当前实例；方法第一个参数；用 `self.xxx` 访问属性
3. **`__init__`**：创建对象时自动调用，初始化属性
4. **`class Contact`** 定义联系人；**`book = AddressBook()`** 创建通讯录；**`self.contacts`** 数据绑对象上，不用到处传参

### D3 函数版 → D6 类版映射

| D3 函数版 | D6 类版 |
|-----------|---------|
| `load_contacts()` | `AddressBook.load()` |
| `save_contacts(contacts)` | `AddressBook.save()` |
| `add_contact(contacts)` | `AddressBook.add()` |
| `delete_contact(contacts)` | `AddressBook.delete()` |
| `find_contact(contacts)` | `AddressBook.find()` |
| `list_contacts(contacts)` | `AddressBook.list_all()` |
| `contacts` 列表（参数到处传） | `self.contacts`（绑在对象上） |
| `{"name", "phone"}` 字典 | `Contact` 类（`name`, `phone`） |

**D6 目标结构**：
```
Contact          → name, phone, show()
AddressBook      → contacts[], load(), save(), add(), delete(), find(), list_all()
main()           → book = AddressBook(DATA_FILE); while 菜单循环...
```

### 块 3 自检 · address_book.py
1. **`while True` + `if/elif`**：菜单一直循环，选 5 `break` 退出；分支调用不同函数
2. **`load_contacts` vs `list_contacts`**：`load` 从 JSON 读进列表、不打印；`list` 接收已有列表 for 打印；`list` 不调用 `load`，由 `main()` 启动时 `contacts = load_contacts()` 再传入
3. **删掉 `save_contacts`**：增删只改内存里的 `contacts`，不写 JSON，退出程序数据丢失

### 代码/实验
- 远程仓库：`https://github.com/huang-CG/ai_agent_learning.git`
- 本地 commit：`212240d` D1 · `9e447b1` D2 · `2c13ab1` D3 · `cd9933c` D4 · `e8e3ece` D5 README · `709f44f` D5 笔记
- 运行验证：`git log --oneline` · `git remote -v` · GitHub 页 5 commits

### 遇到的问题
- 误敲 `git long` / `git pull`（想 push）→ 看错输出即可，pull 无害
- 第一次 push 需浏览器登录 GitHub
- D2–D4 本地 commit 由 Agent 代敲；push / README commit 亲手完成

### 明日预习
- D6：重构通讯录为 OOP 版 → `exercises/day06/`
- 理论：廖雪峰 class / `__init__` / `self`（D5 已预习前 3 节）

---

## Day 4 · Python 进阶（2026-07-04）✅

**时长**：3h | **自评**：3/5 | **Agent 评**：4/5

**Agent 评语**：requests 验收✅；`get_weather` 自主完成度高；中文格式化 + 菜单重构经指导修正（show_menu 缩进、main 分支逻辑）。理论①②已看但来源偏散；语法（if/elif 结构、函数层级）仍是主要卡点，与自评一致。

### 今日进度
- [x] 理论① 模块/pip/site-packages（廖雪峰 9.1–9.2）
- [x] 理论② requests/HTTP GET（廖雪峰 requests 章 + 终端 `-c` 验证）
- [x] 练习：`exercises/day04/weather.py` 成功打印天气 JSON
- [x] 加分：`format_weather` 中文显示（城市/天气/温度/风向/风速/湿度）
- [ ] 拓展：venv 深入（D1 已会，跳过）

### 关键结论
1. **`pip install` → `import`**：第三方库装进 venv 的 site-packages，Python 按搜索路径加载
2. **`requests.get(url, timeout=10)`**：发 GET；`?format=j1` 是 URL 参数；`timeout` 是函数参数，不能写进 URL
3. **`response.json()`**：HTTP 响应体 → Python 字典；嵌套取值如 `weather["current_condition"][0]["temp_C"]`
4. **`dict.get(key, default)`**：安全查表，键不存在返回默认值（风向/天气中译）
5. **`python -c "代码"`**：终端执行一行代码；`python xxx.py` 是运行文件

### 代码/实验
- 文件：`exercises/day04/weather.py`
- 运行：`python exercises/day04/weather.py`
- 验收：输入城市 → 打印 JSON / 中文摘要

### 遇到的问题
- `timeout` 误写进 URL 字符串 → 应为 `requests.get(url, timeout=10)`
- `python "代码"` 缺 `-c` → Python 把字符串当文件名
- 菜单版 `show_menu` 缩进在 `get_weather` 内部；`main` 的 if/elif 逻辑混乱
- 理论来源多（导航+廖雪峰+对话），感觉零碎

### 学习体感 & 调整
- **理论太碎** → 后续 Python 基础统一跟 **廖雪峰 Python 教程** 一条线（9 模块 / 17 第三方），编程导航作补充
- **语法不牢** → 零碎时间继续牛客语法题；写代码前先口述「菜单循环 → 分支 → 调函数」

### 明日预习
- 鱼皮 Git 教程前 4 章
- D5：创建 GitHub 仓库 + 提交 D2–D4 代码

---

## Day 3 · Python 语法（二）（2026-07-03）✅

**时长**：3h | **自评**：4/5 | **Agent 评**：4/5

**Agent 评语**：验收全过；add/delete/find/list 自主完成度高；main 误调 load、break/缩进等经提示后修正；最终理解 load/save 到位。拓展未做，不扣分。

### 今日进度
- [x] 速通 Python 函数 / 异常 / 列表字典相关章节
- [x] 练习：通讯录 CLI `exercises/day03/address_book.py`
- [x] JSON 持久化验收通过（退出再运行数据仍在）
- [x] 增删查 + 显示全部功能通过
- [ ] 拓展：「从 0 到 1 学 Python」函数章节（未做）

### 关键结论
1. **函数拆分**：`load/save` 管文件 · `add/delete/find/list` 管业务 · `main` 管循环
2. **列表 + 字典**：`contacts` 是列表，每个元素 `{"name": ..., "phone": ...}` 是字典
3. **`json.load` / `json.dump`**：Python 列表 ↔ JSON 文件；`load` 启动读，`save` 改完写
4. **`return` vs `break`**：`return` 结束函数；`break` 只跳出循环，不能在函数顶层用
5. **`load_contacts()` ≠ 显示**：读文件不打印；显示要用 `list_contacts(contacts)`

### 代码/实验
- 文件：`exercises/day03/address_book.py` · 数据：`exercises/day03/contacts.json`
- 运行：`python exercises/day03/address_book.py`
- 验收：添加张三 → 显示/查找 → 退出 → 再运行仍在

### 遇到的问题
- `main` 选项 1 误调 `load_contacts()` → 改为 `list_contacts(contacts)`
- `find_contact` 缩进 / `if` 缺冒号
- `delete_contact` 函数外误用 `break`
- 陌生 API（`exists()`）→ 查 `help()` 或 README 参考代码

### 明日预习
- `requests` 库发 HTTP 请求
- D4 练习：调用免费天气 API 打印 JSON

---

## Day 2 · Python 语法（一）（2026-07-02）✅

**时长**：3h | **自评**：2/5 | **Agent 评**：3/5

**Agent 评语**：计算器最终可运行，但题意理解、缩进、main 逻辑需较多指导；概念问得少，动手偏多。达标但独立度偏弱。

### 今日进度
- [x] 鱼皮「十分钟速通 Python」第 1–2 章（变量/类型/条件/循环）
- [x] 练习：命令行计算器 `exercises/day02/calculator.py`
- [x] 学会 Python 交互模式（`>>>`）与运行脚本的区别
- [ ] 拓展：10 道基础语法题（未做）

### 关键结论
1. **Python 缩进 = 代码块**：`def` / `if` / `while` 内的代码必须统一 4 空格，混用会 `IndentationError`
2. **`pass`**：占位符，函数/分支还没写内容时用，有真实代码后删掉
3. **切片 `[start:stop:step]`**：取列表一段，返回新列表，不改原列表；`stop` 不包含
4. **两种运行方式**：`>>>` 试小段代码；`PS>` 下 `python xxx.py` 运行完整脚本
5. **计算器题目本质**：`while` 循环显示菜单 → 用户选 1–5 → 读两个数 → 计算 → 直到选 5 退出

### 代码/实验
- 文件：`exercises/day02/calculator.py`
- 运行：`python exercises/day02/calculator.py`
- 结构：`show_menu()` 打印菜单 · `calculate()` 四则运算 · `main()` 循环调度

### 遇到的问题
- **没理解题目意思** → 菜单是数字 1–5，不是直接输入 `+ - * /`；要用字典把选项映射成运算符
- 在 `>>>` 里输入 `python xxx.py` → SyntaxError（那是 PowerShell 命令）
- 缩进不一致（3 空格 vs 4 空格）→ `IndentationError`

### 明日预习
- 函数参数、列表、字典
- D3 练习：通讯录 CLI（增删查 + JSON 持久化）

---

## Day 1 · 启动日（2026-07-01）✅

**时长**：3h | **自评**：5/5 | **Agent 评**：5/5

**Agent 评语**：环境、认知、笔记、Git 一次性到位；无明显卡点。

### 今日进度
- [x] venv + `.env` + `check_env.py` 通过
- [x] 鱼皮《AI 指南》前 3 章
- [x] AI Agent 路线 + 5 个术语
- [x] 写「什么是 Agent」
- [x] Git 首次 commit（`212240d`）

### AI 指南 · 关键结论（第 1 章原文笔记）
Agent核心概念：
1.大模型是Agent的大脑，不同的模型拥有不同的参数规模、对话上限和处理能力
2.prompt(提示词)输入的质量决定Agent输出的质量，分为系统提示词和用户提示词，前者要提前设置约束Agent，后者自主输入
3.Token可以看作网络世界的价值单位，Token越多，成本越高，输出速度慢
4.API key:你在每个大模型创建的钱包
5.模型蒸馏：大模型难部署，将大模型知识浓缩转移到小模型，保持大模型性能，降低推理成本
6.解析上下文，获取更多的输出依据
7.RAG检索增强：提供外部知识库为Agent补充知识，其中用到Embedding嵌入
8.工具调用使Agent能力边界和应用范围无限扩大
9.CoT(思维链)和ReAct技术：一个将思考推理过程透明化，一个将思考->计划->推理->执行->推理流程循环化
10.智能体工作流：多Agent协同
11.MCP(模型上下文协议)统一Agent调用工具接口的标准
2. 第 2 章：市面上的Agent有很多，它们作用于不同的领域，我们只需按需取用
3. 第 3 章：使用Agent有很多技巧，学习技巧有助于我们更快更好地完成任务

### Agent 路线 · 5 个术语（已理解）

| 术语 | 一句话 |
|------|--------|
| **ReAct** | 推理→行动→观察 循环，Agent 最经典架构 |
| **Function Calling** | 模型输出 JSON 调用函数，程序执行后回传结果 |
| **Reflection** | 执行后自我审视，发现错误再迭代 |
| **Multi-Agent** | 多个 Agent 分工协作完成复杂任务 |
| **Execute** | Plan-and-Execute 中「执行计划」的阶段 |

### 什么是 Agent ✅

> Agent 是基于 LLM 的智能应用系统，能感知、推理、自主决策、执行命令。  
> **聊天机器人**：只会说，不会做。  
> **Agent**：会说也会做——思考、计划、调用工具、反思记忆、迭代优化。

### 代码/实验
- 验收：`python scripts/check_env.py` 通过
- Git：`212240d Day 1: project setup and learning kickoff`

### 遇到的问题
- `copy .env.example.env` 写错 → 应为 `copy .env.example .env`

---

## Day 0 · 规划日（2026-06-30）

### 今日目标
- 确认 90 天 AI Agent 学习路线
- 了解整体阶段划分

### 关键结论
- 路线基于鱼皮编程导航 AI Agent 学习路线，压缩为 90 天
- 技术栈：**Python + LangChain/LangGraph**（零基础最快路径）
- 每日报学习时间，Agent 裁剪当日任务
- 两个文档：`notes/学习笔记.md`（你）+ `CONTEXT.md`（Agent 上下文）

### 已确认（2026-06-30）
- [x] 编程导航 **VIP**（Phase 6 跟 yu-ai-agent）
- [x] API：**DeepSeek**
- [x] 求职：**AI Agent 岗位**
- [x] D1：**2026-07-01**
- [x] 环境：Python 3.11+ / Git / Cursor 已安装

### D1 前待办
- [x] 注册 DeepSeek API Key
- [ ] 开始学习时报今日时长

### 预习完成（2026-06-30）
- DeepSeek API Key 已就绪，D1 可跳过注册步骤

---

<!-- 以下为每日笔记模板，学习后替换占位内容 -->

<!--
## Day X · 主题名（YYYY-MM-DD）

**时长**：Xh

### 今日目标
-

### 关键结论
1.
2.
3.

### 代码/实验
- 仓库：
- 运行方式：

### 遇到的问题
- 问题 → 解决

### 明日预习
-
-->

---

## 术语表

| 术语 | 一句话解释 | 首次出现 |
|------|------------|----------|
| LLM | 大语言模型，根据文本预测下一个词 | D1 |
| Token | 模型处理文本的最小单位，影响计费与长度 | D1 |
| Prompt | 给模型的指令/输入 | D1 |
| Agent | 能感知、推理、调用工具完成任务的 AI 系统 | D1 |
| ReAct | Thought→Action→Observation 循环架构 | D1 |
| RAG | 检索增强生成，先查知识库再回答 | D1 |
| MCP | 模型上下文协议，标准化工具接入 | D1 |
| CoT | 思维链，让模型逐步推理 | D1 |
| Function Calling | 模型结构化调用外部函数 | D1 |
| Reflection | Agent 执行后自我审视并迭代 | D1 |
| Multi-Agent | 多 Agent 分工协作 | D1 |
| 切片 | 用 `[start:stop:step]` 取序列一段，返回新对象 | D2 |
| pass | 空占位，让空代码块语法合法 | D2 |
| json.load/dump | JSON 文件与 Python 对象互转 | D3 |
| requests.get | 发 HTTP GET 请求获取 API 数据 | D4 |
| HTTP GET | 「给我数据」的请求方式；200=成功 | D4 |
| git add | 工作区改动放入暂存区 | D5 |
| git commit | 暂存区快照写入本地仓库 | D5 |
| git push | 本地仓库上传到远程（GitHub） | D5 |
| git pull | 远程拉取并合并到本地 | D5 |
| Class / 类 | 自定义对象类型，相当于模板 | D5 |
| self | 类方法中代表当前实例 | D5 |
| `__init__` | 创建实例时自动调用，初始化属性 | D5 |
| requests.post | 发 HTTP POST 请求，可带 JSON body | D7 |
| HTTP POST | 「提交数据」的请求方式；body 在请求体里 | D7 |
| os.getenv | 从环境变量按名字取值，常配合 `.env` 读配置 | D7 |
| Chat Completions | 大模型对话接口，发 messages 拿回回答 | D7 |
| raise_for_status | 检查 HTTP 状态码，非 2xx 抛异常 | D7 |
| temperature | 控制回答随机性；低=稳，高=发散 | D8 |
| 上下文窗口 | 模型一次能处理的最大 Token 量 | D8 |
| 幻觉 | 模型自信地生成不一定正确的内容 | D8 |
| system prompt | messages 里 role=system，定角色/格式/约束 | D9 |
| Prompt Engineering | 设计 instructions 让模型稳定完成任务 | D9 |

---

## 项目清单

| 项目 | 阶段 | 状态 | 链接 |
|------|------|------|------|
| 命令行计算器 | D2 | ✅ | `exercises/day02/calculator.py` |
| 通讯录 CLI | D3 | ✅ | `exercises/day03/address_book.py` |
| 天气 API 查询 | D4 | ✅ | `exercises/day04/weather.py` |
| AI Agent 学习仓库 | D5 | ✅ | [GitHub](https://github.com/huang-CG/ai_agent_learning) |
| 命令行 AI 问答 | D7 | ✅ | `exercises/day07/chat.py` |
| Prompt 实验台 | D9 | ✅ | `exercises/day09/prompt_lab.py` |
| 手写 ReAct 天气 Agent | D22 | ⬜ | — |
| 智能文档助手 | D42 | ⬜ | — |
| 多 Agent 协作 Demo | D53 | ⬜ | — |
| 毕业综合项目 | D68 | ⬜ | — |

---

## 面试题自测

> Phase 7 填充，每题一行答案链接到对应 Day 笔记

*暂无*

---

## 总复盘

*Day 90 填写*
